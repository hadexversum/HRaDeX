# Export coloring commands for ChimeraX

Export coloring commands for ChimeraX

## Usage

``` r
export_chimera_commands(
  hires_params,
  dominant = FALSE,
  state = hires_params[["State"]][1],
  chain = "A"
)
```
